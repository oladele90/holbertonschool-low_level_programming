#include "main.h"

/**
 * _islower - check to see if letter is lower case.
 *
 * Return: 1 if c is lower case
 */
int _islower(int c)
{
int x;
if (x <= 'z' && x >= 'a')
return (1);
else
return (0);
}
