#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_to_98(int n)
{
  if (n < 98);
  {
  printf("%d, ", n);
  n++;
  }
  if (n > 98);
  {
    printf("%d, ", n);
    n--;
  }
  printf("%d", n);
}
