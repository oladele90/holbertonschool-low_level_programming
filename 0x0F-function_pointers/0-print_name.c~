#include "function_pointers.h"
#include <stdlib.h>
/**
 * print_name - points to function that prints name
 * @f: function that prints name
 * @name: name that will be printed
 */

void print_name(char *name, void (*f)(char *))
{
	if (name == NULL || f == NULL)
		return;
	(f)name;
}
