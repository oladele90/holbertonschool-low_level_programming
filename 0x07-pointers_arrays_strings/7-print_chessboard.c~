#include "main.h"

/**
 * print_chessboard - print a chessboard
 * @a: points to spot in array
 * Return: nothing
 */
void print_chessboard(char (*a)[8])
{
	int i, p;
	for (i = 0; i <= 7; i++)
	{
		for (p = 0; p <= 7; p++)
		{
			_putchar(a[i][p]);
		}
		_putchar('\n');
	}
}
